# -*- mode: Conf; -*-
after_workon = 'git remote update --prune'
priority = 0 # lowest tags run first

# Example:
# after_clone = 'echo after clone from tag'
# after_workon = 'echo after workon from tag'
# priority = 0
# workspace = '/home/other'
# default = false
